export function  displayError (message) {
    return `<div class="error">
                <h3> Unable to retrive products </h3>
                <p class="error-message">${message}</p>
            </div>`
}   